all:
  children:
    piwheels_master:
      hosts:
        piwheels-master:
          ansible_host: # TODO: set master hostname or IP
          ansible_user: root
    piwheels_slaves:
      children:
        cp311:
          # Debian Bookworm (Python 3.11) slaves
          hosts: {}
        cp313:
          # Debian Trixie (Python 3.13) slaves — provisioned as Bookworm, upgraded
          hosts: {}
        cp39:
          # Debian Bullseye (Python 3.9) slaves
          hosts:
            pw-slave-cp39-15:
              ansible_host: ssh.pw-slave-cp39-15.hostedpi.com
              ansible_port: 5273
            pw-slave-cp39-16:
              ansible_host: ssh.pw-slave-cp39-16.hostedpi.com
              ansible_port: 5203
